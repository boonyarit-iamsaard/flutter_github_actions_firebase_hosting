# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

# Integrate Flutter Web with Firebase Hosting
# https://firebase.google.com/docs/hosting/frameworks/flutter

name: Deploy to Firebase Hosting on Preview Channel

on:
  push:
    branches:
      - develop

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Java
        uses: actions/setup-java@v1
        with:
          java-version: '12.x'

      - name: Setup Flutter
        uses: subosito/flutter-action@v1
        with:
          channel: 'stable'

      - name: Get Flutter dependencies
        run: flutter pub get

      - name: Build Flutter Web
        run: flutter build web --release

      - name: Deploy to Firebase Hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_FLUTTER_GITHUB_ACTIONS_A162F }}'
          projectId: flutter-github-actions-a162f
        env:
          FIREBASE_CLI_EXPERIMENTS: webframeworks
